@page
@model SLGIS.Web.Areas.Admin.Pages.Factory.NewOrEditModel
@{
    ViewBag.Title = Model.Factory.Id != Guid.Empty ? "Sửa lại" : "Tạo mới";
    ViewBag.Breadcrumbs = "<a href='" + Url.Page("Index") + "' class='breadcrumb-item'>Factory</a>" +
                          "<span class='breadcrumb-item active'>" + ViewBag.Title + "</span>";

}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="card shadow-base mb-2">
        <div class="card-body">
            <div class="row">
                <div class="col-md-6">
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    @Html.HiddenFor(model => model.Factory.Id)
                    <div class="form-group">
                        @Html.LabelFor(model => model.Factory.Title)
                        @Html.EditorFor(model => model.Factory.Title, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.Factory.Title, "", new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        @Html.LabelFor(model => model.Factory.SendKey)
                        @Html.EditorFor(model => model.Factory.SendKey, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.Factory.SendKey, "", new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        @Html.LabelFor(model => model.Factory.Owner)
                        @Html.EditorFor(model => model.Factory.Owner, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.Factory.Owner, "", new { @class = "text-danger" })
                    </div>
                </div>
            </div>
        </div>
    </div>

    <button class="btn btn-primary" type="submit">Lưu</button>
    <a href="@Url.Page("Index")" class="btn btn-outline-secondary">Quay lại</a>
}

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}